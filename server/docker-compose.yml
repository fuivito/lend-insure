version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: lendinsure-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: lendinsure
      POSTGRES_PASSWORD: lendinsure
      POSTGRES_DB: lendinsure
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lendinsure"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
